pluginManagement {
    repositories {
        //gradlePluginPortal()
        //mavenCentral()
        maven { url 'https://artifactory.tools.post.ch/artifactory/swisspost-maven-artifactory'}
        maven { url 'https://artifactory.tools.post.ch/artifactory/swisspost'}
        maven { url 'https://artifactory.tools.post.ch/artifactory/swisspost-gradle'}
    }
}

rootProject.name = 'akhq'

include 'client'

